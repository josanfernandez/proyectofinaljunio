// Clase Comunidad
export class Comunidad {

  // Atributos de la clase Provincia
  id: number;
  nombre: string;

  // Constructor de la clase Provincia
  constructor(
    id: number,
    nombre: string,
  ) {
    this.id = id;
    this.nombre = nombre;
  }

}